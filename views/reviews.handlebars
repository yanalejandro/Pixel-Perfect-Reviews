<nav class="level">
    <div class="level-item">
      <p class="level-item"><a class="button is-primary" href="/profile">Favorite Reviews</a></p>
    </div>
    <div class="level-item">
      <p class="level-item"><a class="button is-primary" href="/profile/my_reviews">My Reviews</a></p>
    </div>
    <div class="level-item">
      <p class="level-item"><a class="button is-primary" href="/profile/wish_list">My Wish List</a></p>
    </div>

</nav>
<div class="tile is-parent">
        <article class="tile is-child notification is-danger">
          <p class="title">Your Reviews</p>
          {{#if reviews}}
          <div class="section">
            {{#each reviews as |review|}}
            {{> reviewlist}}
            {{/each}}
          </div>
          {{else}}
          <div>You have made no reviews</div>
          {{/if}}
          </figure>
        </article>
    </div>